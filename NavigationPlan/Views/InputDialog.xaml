<Window x:Class="NavigationPlan.Views.InputDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Add Waypoint"
        Width="300" Height="140"
        ResizeMode="NoResize"
        WindowStartupLocation="CenterOwner"
        ShowInTaskbar="False">
    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Waypoint name:" Margin="0,0,0,4"/>
        <TextBox  Grid.Row="1" x:Name="NameBox" Margin="0,0,0,12"/>

        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="OK"     Width="72" IsDefault="True" Click="OkButton_Click"/>
            <Button Content="Cancel" Width="72" IsCancel="True"  Margin="8,0,0,0"
                    Background="#FF3F3F3F" BorderBrush="#FF3F3F3F"/>
        </StackPanel>
    </Grid>
</Window>
