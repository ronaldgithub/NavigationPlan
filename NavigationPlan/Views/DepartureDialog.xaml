<Window x:Class="NavigationPlan.Views.DepartureDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Set Departure Airport"
        Width="300" Height="210"
        ResizeMode="NoResize"
        WindowStartupLocation="CenterOwner"
        ShowInTaskbar="False">
    <Grid Margin="12">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="75"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Label Grid.Row="0" Grid.Column="0" Content="ICAO:"/>
        <TextBox Grid.Row="0" Grid.Column="1" x:Name="NameBox" Margin="0,2,0,4"/>

        <Label Grid.Row="1" Grid.Column="0" Content="Latitude:"/>
        <TextBox Grid.Row="1" Grid.Column="1" x:Name="LatBox" Margin="0,2,0,4"
                 ToolTip="Decimal degrees, e.g. 51.9614"/>

        <Label Grid.Row="2" Grid.Column="0" Content="Longitude:"/>
        <TextBox Grid.Row="2" Grid.Column="1" x:Name="LonBox" Margin="0,2,0,4"
                 ToolTip="Decimal degrees, e.g. 6.8400"/>

        <StackPanel Grid.Row="4" Grid.ColumnSpan="2" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="OK"     Width="72" IsDefault="True" Click="OkButton_Click"/>
            <Button Content="Cancel" Width="72" IsCancel="True"  Margin="8,0,0,0"
                    Background="#FF3F3F3F" BorderBrush="#FF3F3F3F"/>
        </StackPanel>
    </Grid>
</Window>
